name: Deploy Firebase Functions

on:
  push:
    branches:
      - main
    paths:
      - 'functions/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      
      - name: Deploy to Firebase Functions
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          GITHUB_TOKEN_FOR_AI: ${{ secrets.GHAI_TOKEN }}
        run: |
          cd functions
          npm ci
          cd ..
          firebase functions:config:set github.token="$GITHUB_TOKEN_FOR_AI" --token "$FIREBASE_TOKEN" --project whoa-geier
          firebase deploy --only functions --token "$FIREBASE_TOKEN" --project whoa-geier
